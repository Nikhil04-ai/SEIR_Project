# SEIR_Project
A Python program that compares two web pages using 64-bit Simhash. It extracts text, counts word frequencies, applies polynomial rolling hash (p=53, m=2^64), and outputs the number of common bits between the documents.
